</div>
        {
         this.state.posts.length &&

         <ul>
           {this.state.posts.map((post, index) => {
             return (
              <li key={post.id}>{post.title}</li>
             )
           }
            
          )} 
            
         </ul>
       
       }
     </div>